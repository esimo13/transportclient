import Particles from "react-tsparticles";
import particlesconf from "./config/particle";

const PartclesBackground = () => {
  <Particles params={particlesconf}></Particles>;
};

export default PartclesBackground;
